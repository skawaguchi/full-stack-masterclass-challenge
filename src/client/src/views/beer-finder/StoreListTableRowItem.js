import React from 'react';
import { PropTypes } from 'prop-types';

function StoreListTableRowItem() {
    return (
        <tr>
        </tr>
    );
}

StoreListTableRowItem.propTypes = {
    item: PropTypes.shape({
    })
};

export default StoreListTableRowItem;
